// Tipos generados de Supabase (se generarán automáticamente más adelante)
export type Json =
  | string
  | number
  | boolean
  | null
  | { [key: string]: Json | undefined }
  | Json[]

export interface Database {
  public: {
    Tables: {
      // Se completará cuando se generen los tipos de Supabase
    }
  }
}
